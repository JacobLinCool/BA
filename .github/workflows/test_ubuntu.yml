name: 自動化測試 Ubuntu

on:
  workflow_dispatch:

jobs:
  automation:
    name: 自動化 Ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Bahamut Automation
        uses: JacobLinCool/Bahamut-Automation@action
        with:
          modules: "report,ad_handler,login,sign,logout"
          secrets: ${{ secrets.SECRETS }}
